def mark_processing(video_id: str) -> None:
    return None


def mark_completed(video_id: str, output_keys: dict[str, str]) -> None:
    return None


def mark_failed(video_id: str, error_message: str) -> None:
    return None
